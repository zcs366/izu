---
title: 可证伪预测
timestamp: 2026-05-20-0405
---

# 可证伪预测

预测：如果在本周内用CodeBERT对50个Python函数（含语义等价和语义不同的变体）做隐空间聚类，则同类语义函数的类内最大距离 > 异类间最小距离（即分离度 < 1.0），否则本轮预测错误。

依据：EBSE 2025论文已发现代码嵌入模型在捕获功能语义上的局限性，且CodeBERT的MLM预训练不提供显式语义等价信号。张仪（ITA）基于此判断65%概率分离度不达标。本预测给出更强版本：分离度甚至低于1.0（等价函数互不聚类）。
